# 101-res.management.backend
Restorent Management

On first start, the application exposes `GET /setup/status` and the UI shows a restaurant setup form. The form creates the restaurant and its single Owner account in one transaction. Customer registration is disabled; only Owner and staff accounts can sign in.
